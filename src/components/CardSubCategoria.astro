---
import type { SubCategoria } from '../types/menu';

interface Props {
  subcategoria: SubCategoria;
}

const { subcategoria } = Astro.props;
---

<a
  href={`/subcategoria/${subcategoria.slug}`}
  class="block no-underline group"
>
  <article class="bg-white border-4 border-charcoal overflow-hidden transition-all duration-200 group-hover:translate-x-1 group-hover:translate-y-1 hard-shadow group-hover:shadow-none h-full">
    <div class="relative overflow-hidden aspect-[4/3]">
      <img
        src={subcategoria.imagen}
        alt={subcategoria.nombre}
        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
        loading="lazy"
      />
    </div>
    <div class="p-4">
      <h2 class="font-display text-xl font-extrabold text-charcoal title-case mb-1">
        {subcategoria.nombre}
      </h2>
      <p class="font-body text-sm text-charcoal-light">
        {subcategoria.descripcion}
      </p>
    </div>
  </article>
</a>
